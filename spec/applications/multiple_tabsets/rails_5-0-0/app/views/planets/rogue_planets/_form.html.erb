<%= form_for(planets_rogue_planet) do |f| %>
  <% if planets_rogue_planet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planets_rogue_planet.errors.count, "error") %> prohibited this planets_rogue_planet from being saved:</h2>

      <ul>
      <% planets_rogue_planet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
